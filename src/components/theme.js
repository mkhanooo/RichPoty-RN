import { DefaultTheme } from "@react-navigation/native";

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    text: "#677294",
    card: "#fff",
    primary: "#F90217",
    notification: "#A3A3A3",
    error: "pink",
  },
};
export default theme;
